{ unstablePkgs, ... }:

[
  # Unstable packages that don't fit in other categories
  unstablePkgs.libcava     # Audio visualizer
  unstablePkgs.code-cursor # Cursor IDE
  unstablePkgs.sunshine    # Game streaming host (newer version than stable)
]

